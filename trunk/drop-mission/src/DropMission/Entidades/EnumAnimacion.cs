namespace DropMission.Entidades
{
    public enum SpritePlayer
    {
        width = 150,
        height = 100,
        frames = 4
    };

    public enum Frames
    {
        interval = 1000 / 15
    };
}