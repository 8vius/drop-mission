namespace DropMission.Entidades
{
    public enum SpritePlayer
    {
        width = 150,
        height = 100,
        frames = 4
    };

    public enum SpriteWeapon
    {
        width = 150,
        height = 100,
        frames = 5
    };

    public enum SpriteBala
    {
        width = 20,
        height = 20,
    };

    public enum Frames
    {
        interval = 1000 / 15
    };
}